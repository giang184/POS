<h1>Edit question:</h1>

<%= form_for [@survey, @question] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :question %>
  <%= f.text_area :question %>
  <p><%= f.submit %></p>
<% end %>